import Hero from "@/components/Hero";
import React from "react";

const page = () => {
  return (
    <section className="h-[calc(100vh-120px)]">
      <Hero />
    </section>
  );
};

export default page;
